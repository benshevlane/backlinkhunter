import { PagePlaceholder } from '@/components/shared/PagePlaceholder';

export default function SettingsPage() {
  return <PagePlaceholder title="Organisation Settings" description="Organisation profile, plan controls, and admin settings." />;
}
